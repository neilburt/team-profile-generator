// require in a package that has all of the functions you need
// that package can require in the classes

const inquirer  = require("inquirer")

askForManagerInfo();{
  inquirer
    .prompt([

  ]).then(makeManager)
}